pipeline {
    agent any
    stages {
        stage('Build image nginx') {
            steps {
                docker build -t nginx:v1 .
            }
        }

        stage('running image') {
            steps {
                docker run -d --name nginx nginx:v1
            }
        }
    }
}